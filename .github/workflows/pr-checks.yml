# PR gate: run E2E before merge to staging.
# Branch protection should require "E2E" to pass.
name: PR Checks

on:
  pull_request:
    branches: [staging]

jobs:
  e2e:
    name: E2E
    uses: ./.github/workflows/e2e-reusable.yml
    with:
      scheme: nose-staging
    secrets: inherit
